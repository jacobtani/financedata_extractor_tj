defaults: &defaults
  adapter: postgresql
  encoding: unicode
  username: tania
  password: password
  host: localhost
  pool: 25

development:
  <<: *defaults
  database: financedata_dev

test:
  <<: *defaults
  database: financedata_test

production:
  <<: *defaults
  database: financedata_prod